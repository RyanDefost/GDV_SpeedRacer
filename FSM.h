#pragma once
#include "BaseState.h"
#include <vector>
#include <map>

class FSM
{
public:
	FSM();

	void ChangeState();

	//std::map<, BaseState>
	//std::vector<BaseState> stateMap = {};

private:

protected:
};