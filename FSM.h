#pragma once
#include "BaseState.h"
#include <vector>
#include <map>

class FSM
{
public:
	FSM();

	void ChangeState();

	//std::map<, BaseObstacleState>
	//std::vector<BaseObstacleState> stateMap = {};

private:

protected:
};